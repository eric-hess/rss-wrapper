<form method="post" action="{{ path('app_wrapper_delete', {'id': wrapper.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wrapper.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>
